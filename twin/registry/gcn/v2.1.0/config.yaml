batch_size: 32
checkpoint_dir: checkpoints_v2.1.0
data_root: data
device: cuda
dropout: 0.3
hidden_channels: 64
in_channels: 16
learning_rate: 0.001
log_dir: logs_v2.1.0
max_epochs: 50
num_layers: 2
patience: 10
pooling: mean
random_seed: 42
segment_length: 256
stride: 256
test_size: 0.15
use_class_weights: true
use_derived_features: true
val_size: 0.15
weight_decay: 0.0001
